# ENA v0.3.6 External Pre-freeze Machine Revalidation — Pass 2

Status: `EXTERNAL_MACHINE_REVALIDATION / FAIL / PRE_FREEZE`

This record preserves the occurrence truth of the second external machine revalidation. It must not be rewritten as PASS after later repair.

## Exact object tested

- Repository: `guytogay/evolution-native-agent-architecture`
- Old failed source: `79d4562d7e28888617a4063840f2e68f7b570737`
- New source tested: `9e9589126d896cd580a2fc04090b9957208ce779`
- Current subtree observed: `9c928b4c99ae72e53c89978cf1d10b7ea068c182`
- Candidate subtree observed: `169e33787df90f860e76bef3ad083b33b90b824d`
- Base: `f14855fdfd57b975195f0b1c261b754bd3058749`

## External verdict

`PREFREEZE_MACHINE_REVALIDATION_FAIL`

## What passed

- exact Git identity: PASS;
- Current isolation: PASS;
- first-pass E1 `AGENT-ADOPTION-INSTRUCTION.md` correction: PASS;
- v2 evolution-record selftest: PASS, 10 cases;
- inherited `ena_evolve.py selftest`: PASS, 10 cases, schema 1.2;
- full inherited composed regression: PASS:
  - migrated v0.3.2: 10/10;
  - inherited v2: 164/164;
  - closure v2.1: 61/61;
  - unexpected verdicts: 0;
  - uncaught exceptions: 0;
- Python compile: PASS, 7/7;
- strengthened workflow configuration contained the requested validation surfaces;
- first external FAIL history remained preserved;
- baseline remained `freeze_eligible: false`.

## Blocking finding

The aggregate `validate_candidate.py` still exited `1` before its final PASS marker.

The first-pass Agent instruction gate had become reachable and passed. The next LITE identity sentinel then failed because `LITE-ADOPTION-INSTRUCTION.md` clearly represented candidate-only / NOT CURRENT semantics but did not contain the exact sentinel phrase `not an adoption baseline`.

This is the same false-BLOCK class exposed in pass 1, at the next previously unreachable gate.

Therefore no aggregate pre-freeze PASS is inherited from source `9e958912...`.

## Additional finding: ignored bytecode artifact

The external verifier observed that importing `validate_contracts` during `regression_suite.py` created an ignored `tools/__pycache__/...pyc` artifact. The tracked worktree remained clean and the deterministic regression-results file was regenerated byte-identically, but the workflow's previous `git status --porcelain` assertion did not observe ignored bytecode artifacts.

## Corrections after pass 2

Author-side corrections must remain separate from this failure occurrence record:

1. `LITE-ADOPTION-INSTRUCTION.md` is to state explicitly that the candidate simulation surface is **not an adoption baseline**, while retaining `NOT CURRENT` and the v0.3.5 adopter-facing Current boundary.
2. The GitHub workflow is to set `PYTHONDONTWRITEBYTECODE=1` and explicitly fail if any `__pycache__` directory or `*.pyc` file appears under the candidate.
3. A new exact source commit/tree must be externally revalidated. No repaired source may inherit this source's FAIL as PASS.

## Evidence boundary

This revalidation did not establish external-world evidence truth, actual authority, actual recovery, fresh-session salience/application, philosophical correctness of Evolution Ecology, release fitness, Current promotion, or independent semantic acceptance.

Occurrence truth:

> E1 was repaired and the full inherited regression passed 235/235, but the aggregate pre-freeze gate still failed at a second brittle LITE identity sentinel. The candidate remained not freeze-eligible.
