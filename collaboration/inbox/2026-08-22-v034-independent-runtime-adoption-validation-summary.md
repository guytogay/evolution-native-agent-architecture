# ENA v0.3.4-candidate — Fresh Independent Runtime-Adoption Validation Evidence Summary

Date: 2026-08-22

Status: `INDEPENDENT_RUNTIME_ADOPTION_VALIDATION_SUPPORTED_WITH_RESIDUALS`

Validator: fresh WorkBuddy agent runtime session; did not author/reconcile/pre-accept the candidate.

## Original report provenance

User-supplied full report filename:
`ENA-v0.3.4-candidate-INDEPENDENT-VALIDATION.md`

Observed local report length:
`356 lines`

SHA-256 of the full supplied report bytes:
`eb23b97b2e3d155f0b57e326a75567edae0bf42b5e46dd236a5082412bf4ddd0`

This repository record is a decision-relevant summary, not a replacement claim that the full report bytes are stored in Git.

## Frozen target independently verified

- semantic candidate commit: `ccc66233c1abe6778177a38950af1f7bb2356b93`
- candidate-directory tree: `61cb33562626c3b8f590919c87f4637416f1ee8f`
- candidate path: `releases/v0.3.4-candidate/`
- freeze record commit: `d4ce9ebff0f83f47090ffea8e44be9bdd6eb7f68`
- branch: `candidate/v0.3.4-runtime-internalization`
- Draft PR: `#47`

The validator independently reconciled that `61cb3356…` is the candidate-directory subtree, not the commit root tree.

## Inherited equality / regression evidence

The validator independently reported byte identity against v0.3.3 Current for:

- Constitution;
- roles/developmental stages;
- capability map;
- core operational contracts;
- schemas tree;
- templates tree;
- tools tree including validator, fixtures, regression runner;
- contribution protocol.

The inherited 235-case regression suite was executed locally by the validator:

- PASS;
- zero unexpected verdicts;
- zero exceptions;
- inherited 164 cases preserved with zero flips.

This regression evidence supports non-regression of inherited validator/schema semantics; it does not validate the new cross-session runtime-adoption property.

## Supported semantic findings

The validator independently derived and supported the candidate's three-layer model:

1. Persistent ENA Runtime Kernel as hot path;
2. Compiled Local Projection as reusable decision-relevant Host reality;
3. Canonical ENA source as cold path for novelty/ambiguity/staleness/exact semantics.

It also supported:

- `ADOPTION != RETRIEVAL`;
- profiles as runtime governance intensity rather than knowledge editions;
- consequence rather than convenience/fear deciding profile intensity;
- low-consequence reversible local writes need not be HIGH_ASSURANCE;
- small persistent self-mutation can require stronger recovery reasoning;
- backup existence is not restore proof;
- partial backup is not whole-Agent continuity;
- no-real-task onboarding must not manufacture production evidence;
- familiar steady-state work should not repeatedly re-read ENA merely to demonstrate compliance.

## Residual D14 — immutable source identity / kernel drift

Strongest counterexample:

The original candidate's persisted Compiled Local Projection recorded an ENA version/candidate identity but did not require an immutable commit/tree/package digest as the source-integrity anchor.

Risk:

- a mutable label/branch may later resolve to different bytes;
- a transformed/paraphrased persisted kernel may drift while retaining the same human-readable label;
- familiar tasks may not naturally create a contradiction that exposes the drift.

Required smallest sufficient correction:

- persist the immutable canonical source identity actually compiled from;
- mutable branch/version label alone is insufficient as integrity evidence;
- source identity change/conflict/unconfirmability becomes a canonical retrieval/revalidation trigger;
- preserve transformation/source lineage when the persisted kernel is transformed/paraphrased.

## Residual D2 — persistence-boundary claim strength

Original wording required actual-boundary persistence testing only `where decision-critical`.

Required clarification:

Before claiming cross-session/equivalent persistent ENA adoption for decision-critical operation, evidence the actual boundary claimed. A current-session write to a memory/configuration object proves that object was written; it does not prove a genuinely fresh session receives/interprets/applies it.

## Persistence experiment limitation

Validator result:
`PERSISTENCE_TEST_UNAVAILABLE`

The WorkBuddy Host used for this validation did not provide an auto-loaded always-on ENA kernel mechanism that the validator could independently test across a genuinely fresh-session boundary in the same validation run.

Therefore the central fresh-session persistence property remains a field-evidence target even after wording residuals are corrected.

## Optional residual not promoted in immediate correction

The validator suggested optional hardening for self-hosted recovery-root independence. This was not classified as a required semantic correction and should remain a residual/field concern unless further evidence justifies stronger wording/mechanism.

## Reconciliation interpretation

The validator wrote both:

- frozen semantics may proceed to reconciliation;
- Items D14/D2 should be applied before promotion;
- no new semantic candidate needed.

Under ENA immutable-candidate discipline, applying D14/D2 after semantic freeze is itself a material candidate-content change. Therefore the original frozen candidate must remain immutable and the corrections must be represented by a successor candidate identity (`v0.3.4-candidate.1`) with its own freeze and targeted revalidation.

The same validator may perform targeted revalidation as the prior falsifier; no new fresh validator is required for closure of its own findings.

## Disposition

Original `v0.3.4-candidate`:
`FROZEN / SUPPORTED_WITH_RESIDUALS / SUPERSEDED_FOR_CORRECTION / NOT_CURRENT / NOT_MAINLINE / NOT_PROMOTED`

Next semantic target:
`v0.3.4-candidate.1`

Next actor after successor freeze:
`PRIOR_INDEPENDENT_RUNTIME_ADOPTION_FALSIFIER_TARGETED_REVALIDATION`
