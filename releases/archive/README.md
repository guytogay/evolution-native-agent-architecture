# ENA Release Archive

Status: `RELEASE_INDEX / ARCHIVE`

Historical/candidate releases remain part of ENA lineage but are not the current formal baseline.

Google Drive archive:

`My Drive / 10 Projects / ENA - Evolution-Native Agent Architecture / 40 Releases / 90 Archive`

The archive contains older ENA releases, migration records, Candidate Revision artifacts, and legacy release folders.

Rules:

- Archive != deletion.
- Candidate != Mainline.
- A historical version should remain recoverable and identifiable by explicit version/status.
- Migration/promotion history should not be rewritten to make the current version appear to have always existed.
- Old release artifacts may retain historical naming conventions; new artifacts should follow the current structure/naming standard.

Current baseline is indexed at `releases/current/README.md`.
