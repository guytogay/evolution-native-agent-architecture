# ENA v0.3.8 — Read Me First

Status: **CURRENT / FIELD_VALIDATION**

For ordinary adoption, start with only:

1. `ADOPTER-QUICKSTART.md` — human integration guide;
2. `RUNTIME-ADOPTION-KERNEL.md` — compact Agent-facing cues and distinctions;
3. `operational/CUE-INDEX.md` — retrieve a HOW only when a real problem calls for it;
4. `ENFORCEMENT-MAP.yaml` — distinguish guidance, machine guards, external controls, and field claims;
5. semantic fixtures / validators when you need conformance evidence.

Do not permanently load the full HOW/reference/research library.

```text
compact hot semantics
-> cue
-> retrieve relevant HOW
-> lightest applicable Host mechanism
-> act / WAIT / UNKNOWN / REFUSE / NOT_APPLICABLE
-> observe reality
```

ENA is not one giant prompt, not a requirement to implement every bundled reference, and not a claim that prose or schema validation proves the outside world.

Equivalent Host-native mechanisms are legitimate when they preserve the required property and boundary.

```text
MODEL_CUE != MACHINE_GUARD
MACHINE_GUARD != EXTERNAL_TRUTH
TRANSLATED != BEHAVIORALLY_EQUIVALENT
```

Use deeper Constitution, reference, lineage, or research material only when it can change the current decision.

> **The adopter should need less project history, not less protection.**
