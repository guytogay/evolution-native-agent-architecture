# ENA v0.3.14 Lineage

Status: `CURRENT / FIELD_VALIDATION`

- predecessor Current: `v0.3.13`, tree `d8b85adaf31358b141aeb0fae9c1745bef0d26c9`;
- source release branch: `release/v0.3.14`;
- active field stream: Issue `#208`;
- release lane: `R0_ADOPTION_SURFACE_LOCAL_PROJECTION_CUE`;
- primary field finding:
  - `F-208-09_LOCAL_PROJECTION_PERSISTENCE_CUE_MISSING_FROM_MINIMUM_ADOPTION_PATH`.

## Source reality contact

A fresh one-shot adopter review of the v0.3.13 R1 release reconstructed the existing Compiled Local Projection contract correctly from `02-SELF-POSITIONING-AND-LOCAL-PROJECTION.md`, but flagged a usability gap: the Minimum integration path did not explicitly tell an adopter to preserve the smallest reusable Host facts that repeatedly change decisions.

That review otherwise returned `ADOPT_WITH_NONBLOCKING_CLARIFICATIONS` and did not infer mandatory seven-tool installation, continuous self-editing, a universal metric, or a universal runtime. Four bounded wording clarifications were incorporated before v0.3.13 release. The remaining Local Projection discovery gap was independently confirmed during maintainer share-ready readback after v0.3.13 release.

## Successor scope

v0.3.14 surfaces an **existing** semantic contract rather than inventing a new mechanism:

- Quickstart: Minimum integration now tells the adopter to preserve the smallest reusable Local Projection when Host facts repeatedly change decisions;
- Agent bootstrap: the same persistence cue appears in the one-shot adoption path;
- Runtime Kernel: the adoption/Host-change cue preserves decision-relevant Host reality after the bounded operationalization pass;
- supported zh-CN adoption surfaces carry the same decision boundary.

The Local Projection remains what the protected `02-SELF-POSITIONING-AND-LOCAL-PROJECTION.md` already defined: a cache of observed reality, not a shadow ENA baseline and not a self-issued credential. v0.3.14 additionally makes this adoption implication explicit without changing that protected semantic floor.

```text
LOCAL_PROJECTION != SHADOW_ENA_BASELINE
LOCAL_PROJECTION != NEW_AUTHORITY_SOURCE
LOCAL_PROJECTION != MANDATORY_DATABASE
```

No Constitution IDs, evolution stages, evolution-record schemas, core machine semantics, or Minimum Evolution Loop behavior are added or rewritten. v0.3.13 remains immutable predecessor occurrence truth and rollback anchor.

Future corrections that change effective content require a successor version identity.
