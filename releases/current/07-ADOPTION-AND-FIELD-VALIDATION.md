# 7. Adoption and Field Validation — Current

Status: `CURRENT / FIELD_VALIDATION`

Current remains deliberately in reality contact. Field validation is a selection surface for the next successor, not a waiting room before Current.

Continuous field stream: GitHub Issue `#208`.

Stable semantic/adoption surfaces are version-neutral by default. Release-bound identity, changelog, lineage and supported-language projection manifests carry the active numeric version. Historical source/version evidence may still name the version that actually produced it.

```text
COLD_SEMANTIC_SURFACE != RELEASE_ID_LABEL_MAINTENANCE_BURDEN
HISTORICAL_PROVENANCE != ACTIVE_RELEASE_IDENTITY
```

The single-hot-payload contract remains:

```text
DEFAULT_AGENT_HOT_PAYLOAD = RUNTIME-ADOPTION-KERNEL.md
AVAILABLE_RESOURCE != DEFAULT_HOT_PAYLOAD
```

High-value field questions include natural cue -> cold HOW retrieval, enforcement honesty, false-BLOCK avoidance, Host-native mapping, language decision behavior, external consequence truth, and unexpected failure shapes.

Machine checks establish represented consistency only in exercised scope. They do not prove external authority, settlement, recovery, provenance, universal Host fitness, future natural salience, or bilingual behavioral equivalence.

A field finding that is already understood and has a bounded fix should not sit knowingly in shareable Current merely to preserve release ceremony. Preserve the predecessor as occurrence truth and use the smallest justified successor.

```text
FIELD_VALIDATION -> OBSERVE -> SMALLEST_JUSTIFIED_SUCCESSOR
KNOWN_BOUNDED_DEFECT -> FIX_BEFORE_RECOMMENDING_CURRENT
```

> **The product test is whether adopters receive the needed protection with the least sufficient active context and without known decision-bearing defects.**
