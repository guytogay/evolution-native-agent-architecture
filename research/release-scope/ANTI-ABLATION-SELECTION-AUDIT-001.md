# Anti-Ablation Selection Audit 001 — First candidate cargo

Status: `RELEASE_SCOPE_AUDIT / SELECTION_WITHOUT_SILENT_DISSOLUTION / CURRENT_UNCHANGED / VERSION_UNASSIGNED`

Date: 2026-08-27

## Question

Did release-scope selection accidentally erase materially distinct HOW branches merely because they were not selected for the first adopter package?

```text
NOT_SELECTED_FOR_FIRST_CANDIDATE
!= RETIRED
!= DISPROVEN
!= NOT_ENA_RESEARCH
```

This audit checks disposition and durable routing, not historical completeness forever.

## Selected adopter-facing branches

These are carried into first-candidate scope through operational guidance/patterns:

- purpose-relative continuity;
- Standing Input;
- Control Retirement;
- Evolution Commons substrate/protocol alternatives;
- curated Memory Metabolism;
- Host mapping guidance;
- consequence-first/cue-to-HOW routing.

Disposition: `SELECTED_ADOPTER_GUIDANCE`.

No material competing HOW is deleted by selecting these guidance surfaces; the HOW map must continue to show alternative Host realizations.

## Selected machine-reference branches

General optional:

- Retrieval Obligation 0.5;
- WAIT;
- Authority Lease;
- Effect Lifecycle;
- Recovery Adapter.

Advanced/specialized optional:

- Evidence Envelope;
- Evidence Dependency Map;
- Contested Authorship.

Disposition: `SELECTED_OPTIONAL_REFERENCE`.

Selection does not make these universal organs. Host-native alternatives remain valid.

## Deferred machine-reference branch

### Commitment / Settlement recovered reconstruction

Disposition: `DEFERRED_FIRST_CANDIDATE / RETAINED_RESEARCH_LINEAGE`.

Reason: evidence maturity/durability history, not lack of importance.

Preserve in:

- `research/prototypes/commitment-settlement-recovered/`;
- migration-settlement composition research;
- Operational Architecture HOW map and #91/#89 lineage.

No retirement claim is made.

## Runtime routing branches

### Tiny Hot Kernel K-A / K-B / K-C

Disposition: `HOST_PATTERN / FIELD_EVIDENCE_OPEN`.

Reason: router machinery exists but natural fresh-session trigger/salience is not universally established.

Do not select a universal winner by token footprint or authored benchmark alone.

The candidate ships cue/routing semantics and Host pattern guidance; exact resident phenotype remains plural.

## Evolution record representation branches

### Current aggregate v2

Disposition: `SELECTED_CURRENT_SEMANTIC_CARRIER`.

### Progressive occurrence/enrichment representation

Disposition: `RESEARCH_ALTERNATIVE / NOT_FIRST_CANDIDATE_RUNTIME`.

Preserve findings:

```text
CURRENT_STATE_EQUIVALENCE != HISTORY_EQUIVALENCE
```

and lineage-survival requirements in Operational guidance/research.

Not selecting the progressive runtime is not evidence that aggregate snapshot alone is complete for every migration/provenance purpose.

## Projection / compaction branches

- lineage-compaction contract;
- compaction × Retrieval composition;
- progressive projection/history-digest research.

Disposition: `RESEARCH_COMPOSITION_HARNESS / RESULTS_INFORM_GUIDANCE`.

Their result survives in candidate guidance:

```text
SUMMARY_VALID != MATERIAL_USE_READY
COLD_REF_PRESENT != RETRIEVAL_SUFFICIENCY_RESOLVED
```

The full harnesses do not need to ship.

## Execution-surface branches

- provider idempotency;
- target fencing;
- optimistic concurrency;
- status query;
- gateway coverage;
- compensation;
- WAIT.

Disposition: `PLURAL_HOST_HOWS / NOT_ONE_UNIVERSAL_REFERENCE_IMPLEMENTATION`.

Effect Lifecycle carries the shared property and points to Host alternatives. Execution-surface fencing simulator remains research evidence.

Do not collapse these into `use idempotency` or `use CAS`.

## Commons/interoperability branches

Preserved alternatives:

- Git/repository Commons;
- OCI-style content-addressed registry;
- object store + explicit index;
- direct transfer;
- A2A / Host-native active Agent task exchange.

Disposition: `PLURAL_PATTERN_LIBRARY`.

```text
ACTIVE_PROTOCOL != DURABLE_COMMONS
```

No one substrate is selected as universal ENA Commons.

## Memory branches

Preserve separation among:

- compiled memory;
- cold archive;
- retrieval obligation;
- Host-native memory blocks/index/search;
- exact-path fallback;
- provenance/supersession/freshness;
- long-run compiler behavior as field evidence.

Disposition: `CURATED_GUIDANCE + SELECTED_RETRIEVAL_REFERENCE + HOST_PATTERN/RESEARCH_RESIDUALS`.

Do not ship the whole historical Memory Metabolism laboratory.

## Identity / social branches

### Purpose-relative continuity

Selected as procedure.

### Fixed universal trajectory/epoch schema

Disposition: `NOT_JUSTIFIED / DORMANT_OR_HOST_SPECIFIC`.

### Standing Input

Selected as bounded procedure.

### Reputation rehabilitation

Disposition: `FIELD_POLICY_OPEN`.

### Selective legibility / external identity mechanisms

Disposition: `HOST_PATTERN`.

### Global scalar reputation

Disposition: `NOT_SELECTED`; no evidence-backed need to revive as universal mechanism.

## Ecology/resource branches

- MSI / intervention/de-escalation experiments;
- culture/specialization/resource pressure;
- discretionary exploration;
- verification/certainty as purchased risk-control service;
- reputation/market dynamics.

Disposition: `FIELD_OR_MESOCOSM / DORMANT_VARIATION`.

Control Retirement procedure is selected, but its real threshold/value remains environment evidence; no universal numeric threshold is introduced.

## Evidence branches

Evidence Envelope and Dependency Map are selected optional references.

Other evidence mechanisms remain composable/Host-specific:

- external attestation;
- in-toto/SLSA-style provenance;
- OpenTelemetry links/traces;
- Host witness/failure-domain systems;
- source authentication.

Bundling reference carriers does not claim to implement those external truth mechanisms.

## Tooling branches

### Minimal v2 helper

Disposition: `SELECTED_CANDIDATE_DEFAULT_TOOLING_DIRECTION`.

### Legacy v1.2 ena_evolve

Disposition: `LEGACY_COMPATIBILITY / LINEAGE`, not default.

### Narrow legacy Variation-Space repair

Disposition: `NOT_SELECTED_AS_PRIMARY`; it solves one false-BLOCK while leaving generation ambiguity.

The option remains reconstructible from this audit/reconciliation; it is not silently forgotten.

## Language branches

- canonical English authoring source remains;
- zh-CN operational projection is selected for decision-bearing new HOW surfaces;
- full duplicate translation of every machine artifact is not selected;
- behavioral language equivalence remains evidence-open.

Disposition: `SELECTED_PROJECTION_SCOPE + EXPLICIT_PARTIAL_MACHINE_ARTIFACT_TRANSLATION`.

## Retired vs deferred discipline

No branch in this audit is called `RETIRED` merely because it is not first-candidate cargo.

Evidence-backed retirement would require an explicit future disposition that states:

- original failure/problem;
- why no longer needed;
- replacement/supersession or changed applicability;
- retained evidence/lineage location;
- what decision is no longer harmed.

```text
SILENT_DISSOLUTION != EVIDENCE_BACKED_RETIREMENT
```

## Audit result

```text
MATERIAL_HOW_BRANCH_SILENTLY_ERASED_BY_FIRST_CANDIDATE_SELECTION = NONE_FOUND_IN_CURRENT_AUDIT
DEFERRED_AND_HOST_SPECIFIC_BRANCHES_REMAIN_DURABLY_ROUTED = YES
FIRST_CANDIDATE_SELECTION_PRESERVES_PLURAL_HOWS = YES
ARCHAEOLOGY_COMPLETE_FOREVER = NO
CURRENT_CHANGE = NO
```

Reopen this audit if candidate assembly drops a selected pointer, moves research lineage, or turns a bundled reference/Host pattern into an implicit mandatory implementation.
