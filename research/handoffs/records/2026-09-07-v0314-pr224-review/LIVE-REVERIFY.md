# Live Reverify Checklist

Before material writes in the next session:

1. fetch `main` head;
2. read `NOW.md`;
3. read `releases/current/CURRENT-BASELINE.yaml`;
4. read `research/handoffs/CURRENT-HANDOFF.yaml`;
5. fetch PR #224 info, head SHA, draft/open state, changed filenames, latest comments;
6. confirm `.github/workflows/current-immutability.yml` still exists;
7. only then decide the next write.
