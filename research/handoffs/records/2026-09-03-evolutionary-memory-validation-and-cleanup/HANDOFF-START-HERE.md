# ENA Deep Session Handoff — Start Here

Handoff ID: `2026-09-03-evolutionary-memory-validation-and-cleanup`

Status: `DEEP_PROJECT_SESSION_SUCCESSION / COMPLETE / POSTMERGE_READBACK_PASS`

Integration:

- PR: `#178 — Prepare deep session handoff and simplify branch continuation`
- squash merge: `ec9255f147c866de3eeb44e7769285800a603afb`
- postmerge readback: `PASS`

## Why this handoff is deep

The outgoing session spans:

- ENA project simplification consequences;
- evolutionary-memory divergence and convergence;
- two semantic-reachability cleanroom rounds;
- one four-arm inheritance mechanism pilot;
- creation of a comprehensive validation-coverage obligation;
- cleanroom methodology improvements;
- branch-topology cleanup and recovery of unique unmerged research;
- cross-repo method transfer to `human-ai-workbench`;
- planned future `ena-field-guide` boundary.

Losing this session without a structured handoff would make the user reconstruct several rounds of reasoning and negative results.

## Receiver: read in this order

1. `NOW.md`
2. `research/handoffs/CURRENT-HANDOFF.yaml`
3. this file
4. `HANDOFF-MANIFEST.yaml`
5. `PROJECT-STATE.md`
6. `RECENT-THREE-ROUNDS.md`
7. `LESSONS-AND-REMINDERS.md`
8. `REPO-ECOSYSTEM.md`
9. `FILE-CATALOG.md`
10. only then open deeper research/evidence needed for the next action

Do **not** begin with a full repository audit.

### Snapshot/readback note

`PROJECT-STATE.md` is intentionally the detailed project-state snapshot captured while the handoff PR was still being prepared. Do not reinterpret its transitional branch wording as current mutable truth.

For postmerge completion facts use, in order:

1. `NOW.md` for live project/research state;
2. `CURRENT-HANDOFF.yaml` for the completed succession pointer;
3. `HANDOFF-READBACK.md` for PR #178 integration/readback and branch-cleanup completion facts.

This preserves occurrence truth without making a premerge snapshot masquerade as live state.

## Current project state in one paragraph

ENA v0.3.7 remains immutable `CURRENT / FIELD_VALIDATION`; recent evolutionary-memory work is **not** a new release candidate. Two cleanroom reachability rounds (A–H) produced 8/8 baseline-good responses, so no Current reachability repair is justified. The first mechanism-discrimination pilot (I/J/K/L: no inheritance vs full archive vs successful recipe vs boundary+counterexamples) produced 4/4 `MECHANISM_GOOD`, so the stronger claim that boundary-oriented inheritance has superior transfer fitness is **narrowed/not observed**. The research program now advances to **Developmental Inheritance / Minimum Developmental Set (MDS)** while a comprehensive coverage map ensures older branches—including metamemory, sleep/dreaming, adaptation ecology, propagation, decay, purpose-relative selection, and cultural ratchet—cannot silently disappear.

## Exact next consequential research action

Design and preregister a **multi-stage Developmental Inheritance / MDS experiment** that can actually discriminate inheritance representations.

Do **not** repeat the mistake of the I/J/K/L pilot where the common target prompt exposed enough decisive Host semantics that a no-inheritance strong model could solve the task directly.

The next fixture should approximately have:

```text
developmental experience corpus
-> different inheritance/development treatments
-> fresh successor
-> multiple novel structurally related tasks
-> behavior observed before explicit historical retrieval where possible
```

Candidate arms:

- no inheritance control;
- full archive/RAG history;
- bare distilled rules;
- developmental package / MDS: scoped disposition + positive/negative/boundary examples + provenance.

Measure at least:

- transfer before explicit retrieval;
- overgeneralization;
- false activation;
- paralysis/under-action;
- counterexample handling;
- lexical-distance transfer;
- speed/cost;
- provenance retention;
- knowing when **not** to apply the inherited lesson.

Use multiple test tasks, not a single one-shot prompt.

## Do not redo

- Do not reopen semantic-reachability cue-repair arms unless a real contrary failure appears. Baseline did not fail.
- Do not claim boundary-memory superiority from I/J/K/L. The pilot was saturated/null for relative mechanism advantage.
- Do not keep rewriting the Supplier-B fixture until the preferred treatment wins.
- Do not update `releases/current/` from these research results.
- Do not create a new Constitution ID merely because new research language sounds elegant.
- Do not give fresh validators the full project handoff or tell them which hidden surfaces to ignore.
- Do not merge `research/ena-reconstruction` wholesale; its three unique useful files were transplanted by PR #178 and exact blob identity was verified from merged `main`.
- Do not treat old branch refs as required archives.
- Do not create `ena-field-guide` merely to publish current hypotheses; wait for reusable real HOW.

## Related repositories

See `REPO-ECOSYSTEM.md`.

Most important distinction:

```text
ENA = natural-law research/framework
Human-AI Workbench = practical collaboration method
Validation cleanrooms = disposable experimental infrastructure
ena-field-guide = planned future practical ENA HOW, not yet created
```

## Takeover state

The handoff integration and postmerge readback are complete.

Normal continuation authority is `main`, not either handoff branch and not `research/ena-reconstruction`.

The old long-lived research branch and completed handoff branches are branch-cleanup candidates only; their decision-relevant lineage is already durable. See:

`research/branch-cleanup/2026-09-03-BRANCH-CLEANUP-AUDIT.md`

The only remaining manual repository-hygiene action is deleting delete-safe branch refs through GitHub UI/CLI because the connected GitHub tool cannot genuinely delete refs.
