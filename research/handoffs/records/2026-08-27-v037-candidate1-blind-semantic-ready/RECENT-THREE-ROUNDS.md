# Recent decision-bearing rounds

## Round 1 — candidate.1 exact pre-freeze and freeze

Candidate.1 successor repairs were reconciled, then exact pre-freeze run `33055811978` passed on one bound identity:

- source `ae6903464133cb5bcf3cd8909ecae1215fe0b9ba`
- subtree `c0458e0d7ea417b841cbf4c8bf6e64e4aff37319`

The candidate was externally frozen without rewriting tested cargo. Current stayed v0.3.6.

Post-freeze reconciliation judged one fresh independent successor review epistemically worthwhile.

## Round 2 — first candidate.1 blind intake invalidated

Issue #128 instructed a fresh reviewer to inspect the full candidate-local subtree while avoiding external author context.

The reviewer opened permitted `CANDIDATE-BASELINE.yaml` and discovered predecessor/repair information inside the candidate itself. The reviewer correctly stopped and declared the reviewer state ineligible for fresh Phase A.

No report was sealed; no candidate byte was modified.

The project recognized a new method defect:

`CANDIDATE_LOCAL != AUTOMATICALLY_BLIND_SAFE`

Issue #128 was closed `not_planned` and preserved as invalid-intake occurrence truth.

## Round 3 — blind semantic view and A-S/A-P split

A contamination audit found candidate-local history/oracle surfaces beyond the baseline file, including changelog/lineage, expected fixtures, regression/selftest corpora, and explicit prior-probe scripts.

The canonical independent-validation method was repaired to distinguish:

- A-S: blind semantic falsification on a priming-reduced exact-byte projection;
- A-P: post-seal independent audit of withheld package/oracle/self-description surfaces;
- Phase B: author/project-manager reconciliation only after independent artifacts persist.

A new branch was constructed from the exact frozen source:

`validation/v037-c1-blind-semantic-primary`

Candidate-side differences are declared removals only; retained candidate files are byte-identical. One mixed-role validator remains exact and has a declared withheld selftest source range for A-S.

New intake:

Issue `#131 — Fresh independent A-S/A-P — v0.3.7 candidate.1`

Candidate.2 remains unwarranted unless the independent work establishes a material candidate-byte correction requirement.
