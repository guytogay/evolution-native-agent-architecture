# ENA Reconstruction Research Generation — 2026-08-26

Status: `ACTIVE_SUCCESSOR_GENERATION / NOT_CURRENT / NOT_RELEASE_AUTHORITY`

This record marks the research-generation boundary created after PR #82 was checkpointed into `main`.

## Lineage

Previous active integration branch:

`research/memory-metabolism-prototype`

Checkpoint PR:

`#82 — ENA reconstruction research checkpoint: executable reference organs and anti-ablation lineage`

Checkpoint merge commit:

`be776b9f8e7d280e39a5389148722229569a2c49`

Successor active integration branch:

`research/ena-reconstruction`

The successor branch was created from the exact checkpointed `main` commit above. It is therefore a new research generation, not a renamed continuation whose ancestry must be guessed.

## Why a new generation exists

The predecessor branch began as a Memory Metabolism prototype and accumulated a much broader ENA reconstruction surface. Once that research was durable enough to checkpoint, continuing indefinitely on the legacy topic-named branch would have created a de facto second main and a misleading research identity.

The new branch names the actual scope:

- anti-ablation archaeology;
- WHAT -> WHY -> HOW -> EVIDENCE reconstruction;
- concrete and plural HOW growth;
- external mechanism harvesting;
- Host binding and cross-organ composition;
- operational architecture assembly when evidence supports it;
- eventual release-scope reconciliation without pre-assigning a release delta.

## Generation invariant

```text
RESEARCH_CHECKPOINT_MERGED_TO_MAIN
!= CURRENT_PROMOTED
!= RELEASE_CANDIDATE_CREATED
```

```text
PREVIOUS_BRANCH_RETIRED
!= PREVIOUS_RESEARCH_ERASED
```

Git history, PR #82, Issues #88–#94, and checkpointed repository artifacts preserve the predecessor generation.

## Continuation rule

Normal continuation must discover the active integration branch from `main/research/ACTIVE-RESEARCH.yaml` after the control-plane pointer transition is merged. Branch name, recency, or commit count must not be used as authority.

The successor continues under the canonical methodology on `main/research/methodology/` and the detailed active research state in `research/plans/PROGRESS.yaml`.

`CURRENT_CHANGE = NO`

> **Checkpoint the tree, preserve the rings, grow the next branch from the new trunk.**
