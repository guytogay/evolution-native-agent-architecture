# Review 04 — Independent Retrieval-Lifecycle Review

Status: `RESEARCH_EVIDENCE / NOT_CURRENT_BASELINE / NOT_AUTHORITY`

Reviewer context: fresh temporary ChatGPT conversation, instructed to perform blind Phase A inspection before ENA reconciliation.

Frozen target:

`2389bc45bf88a0fc250a9a8d191b80b5cc2e0c32`

Primary target:

`research/prototypes/memory-metabolism/retrieval-obligation-0.4/`

Secondary target:

`research/prototypes/memory-metabolism/host-integration-0.1/`

Final verdict:

`NEEDS_NARROW_REVISION`

Key independent findings:

1. stable `returned_record_ids` did not bind effective content/version;
2. fingerprint over-bound semantic sets to array order;
3. every numerically later discovery invalidated closure even when non-material;
4. physical interpretation of `resolver_ref` could false-block legitimate failover;
5. subject fingerprint authenticated no evaluator authority;
6. negative completeness assertions carried an analogous Host trust boundary;
7. restore/current-world freshness remained a continuity problem, not a retrieval-specific one;
8. no hidden `N memories -> O(N) hot state` requirement was found;
9. no new ENA Current rule was justified;
10. another same-layer retrieval reviewer was not recommended after reconciliation.

Reconciled in:

`research/prototypes/memory-metabolism/retrieval-obligation-0.5/`

The full original review was supplied as conversation evidence and should be treated as the source record; this repository note preserves its materially distinct findings and disposition without rewriting the reviewed 0.4 target.
