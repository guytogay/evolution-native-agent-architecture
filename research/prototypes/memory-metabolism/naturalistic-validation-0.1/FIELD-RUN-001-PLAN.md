# Field Run 001 Plan — Existing #72 S17 Maintenance Task

Status: `PRE_REGISTERED_NATURALISTIC_FIELD_RUN / NOT_EXECUTED`

Frozen start commit:

`73b57f8990b50f7335940b4c439165e13197c1da`

The working Host should begin from this project state and record it as the run baseline. Do not silently substitute a later branch head during the run unless the task itself explicitly requires comparison with later state.

## Why this task qualifies

The task source predates Naturalistic Validation 0.1:

- Issue #72, seed S17 — inherited `ena_evolve.py` latent-path false-BLOCK.

It is an ordinary repository maintenance/research question:

> Does the inherited reference tool still unnecessarily require `--variation-space` for legal v0.3.6 latent-now / experiment-later semantics, and if so what is the smallest justified repair direction?

The task was not invented as a retrieval puzzle and has project value even if Memory Metabolism research did not exist.

## Intended Host

Fresh ChatGPT Temporary Conversation or another fresh Host with:

- no prior ENA session history loaded;
- public/connected GitHub access to the repository;
- only a bounded generic retrieval reflex in hot instructions;
- no full domain catalog;
- no expected answer or target-file list supplied.

Eligible evidence class if those conditions hold:

`PRIMARY_NATURALISTIC`

If substantial project history is already loaded, downgrade to:

`CONTEXT_CONTAMINATED_FIELD`.

## Working-Agent bootstrap

The working Agent may be told only this generic project-memory behavior:

> Durable project history exists in the repository but is not fully loaded. Before committing a decision, inspect durable project state when omission of the past could materially change correctness, authority, consequence, recovery, or applicability and the current loaded surface is not known complete. Repeated failure, interruption/restore, or material Host/tool/environment change can also make history relevant. Do not retrieve merely because history exists, and do not preload the whole repository. Use bounded repository search/fetch as the cold project-memory resolver when needed.

This is the generic R0/cold-resolver candidate. It must not include a topic-specific cue list.

## Task given to working Agent

> Repository: `guytogay/evolution-native-agent-architecture`
>
> Frozen start commit: `73b57f8990b50f7335940b4c439165e13197c1da`
>
> Work on the existing maintenance/research seed in Issue #72 S17 concerning the inherited `ena_evolve.py` latent-path false-BLOCK. Independently inspect the frozen repository state and determine whether the reference tool still unnecessarily requires `--variation-space` for behavior that v0.3.6 Current legitimately permits to remain latent until a later experiment. If the issue is real, identify the smallest repair direction and the evidence needed before changing anything. If it is not real, explain why. Do not modify `releases/current/`, do not implement a fix, and do not infer that a next release is authorized.

## Information intentionally NOT supplied

Do not tell the working Agent:

- which source files besides Issue #72 to inspect;
- which Current sections supposedly resolve the question;
- whether S17 is expected to be confirmed or falsified;
- any expected retrieval sequence;
- any hidden oracle;
- the author's preferred repair.

## Evaluation

The evaluator observes only after the task is completed.

Record:

- whether repository history was invoked beyond the explicit Issue seed;
- scopes/files/issues actually consulted;
- whether bounded expansion occurred;
- whether retrieved history changed the conclusion;
- any later challenge/correction evidence;
- operational cost where visible.

Do not score the Agent against an author-written answer key.

A failure classification requires later/recoverable evidence showing where a material dependency was missed or misapplied.

## Stopping discipline

One clean run is useful as a field trace, not a population claim.

Do not repeat the identical task across many models merely to collect answer diversity.

A second Host/run is justified only if Run 001 exposes a mechanism whose Host dependence matters, or if a materially different naturally motivated task is available.
