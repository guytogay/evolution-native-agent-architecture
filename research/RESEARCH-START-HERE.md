# ENA Research — Start Here

Status: `RESEARCH_BOOTSTRAP / HOT_ENTRYPOINT / NON_NORMATIVE / NOT_RELEASE_AUTHORITY`

Purpose: give a fresh research session the **smallest sufficient entrypoint** for continuing ENA without reconstructing the research methodology from hundreds of commits, Issues, comments, or prior chat exports.

This file is a retrieval/bootstrap surface, not a compressed replacement for the research history or methodology.

```text
BOOTSTRAP != COMPLETE_RESEARCH_STATE
HOT_ENTRYPOINT != ONTOLOGY
POINTER != SUBSTITUTE_FOR_COLD_SOURCE
```

## Required read order before substantive ENA continuation

1. Verify the actual Current adoption identity from `releases/current/CURRENT-BASELINE.yaml` on the default branch. Do not trust a hard-coded version copied into an old research document.
2. Read `research/methodology/ENA-RESEARCH-DISCIPLINE.md` on the active research branch.
3. Read PR #82 (`ENA reconstruction: executable reference organs and anti-ablation research`) for the current reconstruction surface and branch state.
4. Read #89 as the master reconstruction ledger, then only the relevant workstreams #90–#94.
5. Read #88 when any conclusion uses words such as `already covered`, `no Core delta`, `Host-specific`, `reference organ`, or `no release change`.
6. If inheriting from a prior ChatGPT/session export, read the latest decision/methodology-bearing tail rather than relying only on a compressed handoff summary.

Only after these reads should a new research node select a concrete next action.

## Current research posture

The current reconstruction is **not** primarily a search for more abstract Constitution prose.

The working structural direction is:

```text
TELOS / PURPOSE
      |
      v
WHAT / WHY        <- semantic trunk; abstraction/compression may be useful
      |
      +--> HOW-A  <- concrete organ/process/tool/procedure
      |     +--> Host binding / adapter
      |     +--> failure/fallback behavior
      |     +--> branch-scoped evidence
      |
      +--> HOW-B  <- materially different concrete path
      |
      +--> HOW-C  <- Host-conditional / experimental path
      |
      +--> ...
```

Key constraints:

```text
PROPERTY != ORGAN != HOST_BINDING != EVIDENCE != ADOPTION
ONE_WHAT_WHY -> 0..N_CONCRETE_HOW_BRANCHES
REQUESTED_N != DISCOVERED_N
CURRENT_CATEGORY_SET != ONTOLOGY
PROTOTYPED != MORE_FUNDAMENTAL
UNPROTOTYPED != UNIMPORTANT
UNSELECTED != ABSENT
```

> **Compress the semantic trunk; let concrete HOWs branch.**

The number, depth, and boundary of HOW branches are research results, not directory slots.

## Reconstruction-phase discipline

During anti-ablation reconstruction:

```text
RECOVER VARIATION
-> preserve durable lineage
-> reconstruct WHAT / WHY / HOW / EVIDENCE
-> only then select, merge, retire, specialize, or promote where justified
```

Do not choose the "most important next organ" merely because it is already visible, prototyped, or easy to engineer. That creates selection-induced ablation of less visible historical HOWs.

A better new organ does not erase the old HOW lineage. Evidence-backed retirement is allowed; silent dissolution is not.

## Research action gate

Before a substantial action, verify at least:

```text
Am I explaining a problem or solving it?
Am I treating a parent property as proof that a concrete problem is solved?
Am I preserving plural/Host-specific/failed/dormant HOW lineage?
Am I selecting before enough variation has been recovered for this decision?
Am I freezing a convenient count, taxonomy, or organ boundary into ontology?
If I call something a HOW, can a fresh Agent actually perform or instantiate it?
Can the proposed experiment reveal structure that cannot already be derived statically?
What exact decision could the next evidence change?
Could NO_CHANGE, dormancy, simplification, or multiple coexisting HOWs be correct?
```

The checklist is open-cardinality. The canonical working methodology is the methodology file, not this abbreviated bootstrap.

## Evidence activation check

A research method being written somewhere is not enough:

```text
WRITTEN -> RETRIEVED -> INTERPRETED -> SALIENT -> APPLIED
```

For session inheritance, explicitly distinguish:

```text
DURABLE != DISCOVERABLE
DISCOVERABLE != RETRIEVED
RETRIEVED != SALIENT
SALIENT != APPLIED
```

A new session that can quote the methodology but immediately performs a forbidden compression/selection has **not inherited the methodology operationally**.

## Record-first continuity

Material changes to research methodology, reconstruction phase, major HOW families, evidence posture, or stop rules must be persisted to GitHub before handoff.

Prefer updating durable entrypoints/pointers over assuming a future session will rediscover a crucial rule from old comments.

Do not duplicate the full cold methodology into every entrypoint. Keep this file small enough to be read first and use it to route into the canonical cold sources.

## Final inheritance test

Before claiming a new session has successfully inherited the ENA research state, it should be able to state, from persisted sources rather than memory alone:

- what the Current adoption baseline actually is;
- what phase the reconstruction is in;
- why WHAT/WHY coverage does not imply practical closure;
- why HOW is open-cardinality and may branch into multiple concrete mechanisms;
- why #90–#94 are organizational shelves rather than an ontology;
- where the full research methodology lives;
- which durable ledger/PR contains current research work;
- and what next action is permitted without silently dissolving historical variation.

> **Do not merely inherit conclusions. Inherit the method that governs how new conclusions may be produced.**
