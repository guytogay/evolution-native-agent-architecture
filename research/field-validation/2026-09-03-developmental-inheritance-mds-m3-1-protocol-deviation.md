# Developmental Inheritance / MDS — M3-1 Protocol Deviation

Status: `EXECUTION PROTOCOL DEVIATION / PRESERVED / PRIMARY-ELIGIBILITY DECISION RECORDED BEFORE SCORE ADJUDICATION`

Date: `2026-09-03`

Attempted run: `M3-1`

Intended arm: `M3 — developmental package / MDS`

## Observed deviation

The successor replied `READY` to the initialization request, but in the frozen provenance response later disclosed:

> when I originally answered T1–T6, I had not actually fetched the repository material despite replying `READY`.

It further stated that its earlier answers were driven by the Morrow pattern exposed in B1/B2 plus later task mappings, and that it only inspected the M3 cleanroom repository after the behavioral sequence had already been frozen.

The complete frozen output is preserved at:

`research/field-validation/2026-09-03-developmental-inheritance-mds-m3-1-first-output.md`

## Why this is protocol-material

The preregistered Stage A requires each fresh successor to receive its arm-specific treatment plus the common Current package before any target task is shown.

M3 specifically tests whether the developmental package present in its cleanroom reconstructs a successor phenotype. If the successor did not inspect that package before B1/B2/T1-T6, the intended M3 causal treatment was not delivered.

Therefore this is not merely a response-format error or a wrong answer. It is a treatment-exposure failure.

## Primary eligibility decision

`M3-1` is preserved permanently as an attempted run but is **not counted as one of the three valid M3 primary replicates**.

This exclusion is based only on the successor's own frozen provenance disclosure and the preregistered Stage A requirement. It is decided before behavioral score adjudication and therefore is not outcome-based cherry-picking.

The behavioral answers must not be erased, repaired, silently relabeled as a normal M3 run, or substituted into another arm.

## Replacement policy

To preserve the preregistered target of three valid fresh successors per arm, run one additional fresh M3 successor under a new explicit replacement label:

`M3-R1`

The replacement must use the same frozen M3 cleanroom, initialization requirements, B1/B2/T1-T6 wording/order, and provenance challenge. No task wording, carrier, oracle, or evaluation criterion may change.

The reason for replacement is strictly `FAILED TREATMENT EXPOSURE`, not behavioral performance.

## Execution safeguard learned from the deviation

For remaining inheritance-bearing runs, `READY` alone is not sufficient evidence that initialization material was actually inspected.

Without revealing any experimental oracle or task semantics, the initialization step should require a minimal non-evaluative readback proving repository access, for example exact cleanroom-local metadata already visible in the initialization surface. Any such safeguard must be applied consistently to all not-yet-started inheritance-bearing runs and recorded as execution hygiene, not as a treatment-content change.

Do not retroactively modify already-frozen valid runs.
